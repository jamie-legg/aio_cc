# Example secrets file - DO NOT commit real secrets!
# Create actual secrets using: kubectl create secret generic content-creation-secrets --from-literal=...

apiVersion: v1
kind: Secret
metadata:
  name: content-creation-secrets
type: Opaque
stringData:
  database-url: "postgresql://user:password@postgres:5432/content_creation"
  redis-url: "redis://redis:6379/0"
  secret-key: "your-secret-key-generate-with-openssl-rand-hex-32"
  openai-api-key: "sk-your-openai-api-key"
  postgres-user: "content_user"
  postgres-password: "change-me-in-production"
  stripe-secret-key: "sk_test_your-stripe-key"
  stripe-webhook-secret: "whsec_your-webhook-secret"

